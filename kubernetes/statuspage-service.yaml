apiVersion: v1
kind: Service
metadata:
  labels:
    app: statuspage
  name: statuspage
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 9000
  selector:
    run: statuspage
  type: NodePort
